fragment UserDetails on User {
  lastName
}

query UserQuery {
  username(name: "edvin") {
    name
  }
}

query UserQueryDeferred1(
  $id: ID!
) {
  node(id: $id) {
    ... on User {
      lastName
    }
  }
}
